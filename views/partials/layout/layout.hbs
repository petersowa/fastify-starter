<!DOCTYPE html>
<html lang="en">

<head>
	{{> head}}
</head>

<body>
	<div class="body-content">
		{{> header}}
		<div class="v-flex-container">
			{{!-- {{> modal}} --}}
			{{{ body }}}
		</div>
		{{> footer}}
	</div>
</body>

</html>